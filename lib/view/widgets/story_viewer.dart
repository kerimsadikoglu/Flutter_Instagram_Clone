import 'package:flutter/material.dart';

class StoryViewer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      // Burada hikaye görüntüleyici arayüzünü oluşturun
    );
  }
}